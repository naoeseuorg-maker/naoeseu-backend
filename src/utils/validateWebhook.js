module.exports = function validateWebhook(req) {
  // Validação avançada será adicionada quando a fábrica ativar a API completa
  // No momento, só retornamos true para permitir testes
  return true;
};
